@props(['bg'=>'bg-primary'])
<button type="submit" id="submit"
        class="rounded-sm {{ $bg }} px-4 py-1.5 text-base font-semibold  text-white border-primary border border-2 hover:{{$bg}}-light">
    Submit
</button>
